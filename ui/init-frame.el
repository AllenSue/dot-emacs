;;; init-frame.el --- Frames initialization   -*- lexical-binding: t -*-

;;; Commentary:

;;; Code:

(toggle-frame-maximized)

;; title
(setq frame-title-format '("合抱之木，生于毫末；九层之台，起于累土；千里之行，始于足下。"))

(provide 'init-frame)

;;; init-frame ends here
